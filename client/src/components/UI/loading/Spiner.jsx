import React from 'react';
import styles from './spiner.css';

const Spinner = () => {
    return (
        <div className={styles.spinner}>
            Загрузка
        </div>
    );
};

export default Spinner;